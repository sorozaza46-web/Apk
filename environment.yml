name: fishing-bot-env
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - numpy
  - pillow
  - pip
  - pip:
    - ultralytics
    - customtkinter
    - pyautogui
    - pyinstaller
    - opencv-python-headless  # 💡 OpenCV dll hatalarını kökten çözen headless sürüm
    
